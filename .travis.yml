language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hapi' ]; then cd .. && eval "mv $currentfolder generator-hapi" && cd generator-hapi; fi

