language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hapijs' ]; then cd .. && eval "mv $currentfolder generator-hapijs" && cd generator-hapijs; fi

